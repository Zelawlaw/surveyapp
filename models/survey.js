const mongoose = require('mongoose');
const Schema = mongoose.Sgitchema;

let surveySchema = schemaDetails ({
    name: 'string',
    phoneNumber: Number,
    gender: 'string',
    birthday: 'string',
    employmentStatus: 'string',
    networth: 'string',
    do_you_own_a_home: 'string'

});

module.exports = schemaDetails;
